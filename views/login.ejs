 <!-- Файл: login.html -->
<!DOCTYPE html>
<html lang="ru">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title><%= t('login.title') %> — KidSpeech PRO</title>
  <link rel="stylesheet" href="public/css/styles.css">
</head>
<body>
  <main class="form-page">
    <h2><%= t('login.title') %></h2>
    <form id="loginForm" action="/api/login" method="POST">
      <input type="text" name="login" placeholder="<%= t('login.placeholder_login') %>" required>
      <input type="password" name="password" placeholder="<%= t('login.placeholder_password') %>" required>
      <button type="submit" class="btn"><%= t('login.button') %></button>
    </form>
    <p><%= t('login.no_account') %> <a href="/register"><%= t('login.register_link') %></a></p>
    <p><a href="/">← <%= t('login.to_main') %></a></p>
  </main>
  <script>
    if (window.location.search.includes('error=1')) {
      alert('<%= t('login.error') %>');
    }
  </script>
</body>
</html>